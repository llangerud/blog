const router = require('express').Router();
const { Comment } = require ('../../models')

//comments will need post, put and delete routes, and will need to check for whether or not user is logged in. 




module.exports = router;